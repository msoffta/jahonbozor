# ====================================
# Database (Docker Compose + Prisma)
# ====================================
# Docker Compose credentials
DATABASE_USER=YOUR_DATABASE_USER
DATABASE_PASSWORD=YOUR_DATABASE_PASSWORD
DATABASE_NAME=YOUR_DATABASE_NAME

# Prisma connection string (must match Docker credentials above)
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/DATABASE

# ====================================
# Backend — Authentication
# ====================================
# Secret key for JWT token signing (generate with: openssl rand -base64 32)
JWT_SECRET=YOUR_JWT_SECRET

# Environment mode (development | production) — affects cookie secure flag
NODE_ENV=development

# Telegram bot token for hash validation (get from @BotFather)
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN

# ====================================
# Backend — Monitoring
# ====================================
# Sentry DSN for error tracking (optional)
SENTRY_DSN=

# ====================================
# Bot Service
# ====================================
# Webhook URL for Telegram bot (e.g. https://yourdomain.com/bot or ngrok URL for dev)
TELEGRAM_WEBHOOK_URL=https://yourdomain.com/bot

# Bot service port (default: 3001)
BOT_PORT=3001

# ====================================
# Frontend (Vite — VITE_ prefix required)
# ====================================
# Telegram bot username for login widget (e.g. @my_bot)
VITE_TELEGRAM_BOT_USERNAME=@YOUR_BOT_USERNAME
